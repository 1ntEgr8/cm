cm-install

Installs and configures software declared in a formula

USAGE:
    cm install [<formula-name> ...]
        Installs and configures each formula.

    cm install --dry-run [<formula-name> ...]
        Lists the formula that will be installed in the order in which they
        will be executed

OPTIONS:
    -v, --verbose   Verbose output 
    --dry-run       Lists formula that will be installed (in the order of processing)

NOTES:
    Installation order:
        - DFS (conflicts resolved using lexicographical order)
